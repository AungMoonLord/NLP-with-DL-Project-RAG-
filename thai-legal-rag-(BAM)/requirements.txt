# Thai Legal RAG - dependencies
# ASCII only: pip on Windows reads this file with the system locale codec (cp874),
# so non-ASCII comments break parsing before any download starts.

# --- core ---
numpy>=1.26
pyyaml>=6.0
tqdm>=4.66

# --- embedding / retrieval ---
torch>=2.2
sentence-transformers>=3.0
transformers>=4.40
faiss-cpu>=1.8
rank-bm25>=0.2.2

# --- Thai language processing ---
pythainlp>=5.0
python-crfsuite>=0.9.9

# --- evaluation ---
bert-score>=0.3.13
matplotlib>=3.8
pandas>=2.2

# --- generation backend (install only what you use) ---
anthropic>=0.40
openai>=1.40
fastapi
uvicorn[standard]

# --- input file formats ---
pypdf>=4.2