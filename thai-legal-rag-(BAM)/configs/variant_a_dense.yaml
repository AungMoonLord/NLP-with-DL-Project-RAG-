raw_dir: data/clean
# Variant A — Dense-only (baseline ของ ablation)
# ตัวแปรที่ควบคุม: chunk 512/64, top_k_final 5, generator เดียวกัน
name: A_dense_only
retrieval:
  mode: dense
  use_reranker: false
  top_k_final: 5
generator:
  backend: openai
  model: gemma-4-E4B-it
evaluation:
  judge_backend: openai
  judge_model: gemma-4-E4B-it
