raw_dir: data/clean
# Variant C — Hybrid + cross-encoder reranking
# ต่างจาก B แค่ "ขั้นจัดอันดับใหม่" อย่างเดียว
name: C_hybrid_rerank
retrieval:
  mode: hybrid
  use_reranker: true
  rerank_candidates: 20
  top_k_final: 5
generator:
  backend: openai
  model: gemma-4-E4B-it
evaluation:
  judge_backend: openai
  judge_model: gemma-4-E4B-it
